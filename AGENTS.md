# Working in this repository

## 目的

このリポジトリは、CodexがWindows上のMinecraftを画面から観察・操作し、シングルプレイのサバイバル／ノーマルでエンダードラゴン討伐を目指すためのものです。ユーザーへの応答とプレイ記録は日本語で行います。

ゲームプレイには [Minecraft gameplay skill](.agents/skills/minecraft-gameplay/SKILL.md) を使い、過去の記録より新しいゲーム画面を優先してください。

## 変えてはならない境界

- GUI入力の送信先は、選択済みのMinecraftゲームウィンドウだけです。デスクトップの他アプリ、ブラウザ、Explorer、クリップボードには入力しません。
- ファイルの作成・変更はこのリポジトリ内だけです。ネット調査にはCodexのWeb検索・取得を使い、シェルのネットワークや外部コネクターはユーザーが別途依頼しない限り使いません。
- [ゲームルール](.agents/skills/minecraft-gameplay/references/game-rules.md) を守ります。
- ゲーム入力はskill内の `runtime/input_boundary.py` を通します。実装を変更しても、Minecraftのウィンドウ／プロセス／前面状態の確認、ゲーム外クリック拒否、停止、入力解放、watchdog、単一入力所有者という不変条件を保ちます。

## 境界内の裁量

上の境界内では、目標達成まで自律的に観察・試行・復旧してください。可逆的なゲーム操作やリポジトリ内変更について、不要な確認待ちはしません。

攻略に役立つ場合は、AGENTS、skill、参照資料、操作・観察・画像分析・計画・記録用コードを作成、変更、置換できます。既存ツールや固定の操作粒度に固執する必要はありません。危険度と不確実性に応じて観察頻度を変え、想定外の状態では入力を止めて再観察します。

runtimeを変更するときは、そのディレクトリの `AGENTS.md` に従い、同ディレクトリで `python -m unittest -q` を実行してください。skillを変更したときは、skill-creatorの検証スクリプトが利用できれば実行します。
